import 'package:flutter/material.dart';

class SIcon extends StatelessWidget {
  const SIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.thumb_up,
      color: Colors.blue,
    );
  }
}
