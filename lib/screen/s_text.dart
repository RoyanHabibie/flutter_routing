import 'package:flutter/material.dart';

class SText extends StatelessWidget {
  const SText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Ini adalah Text Widget');
  }
}
